import { View } from 'react-native';

// This is a placeholder screen that never renders
// The "More" tab in the bottom navigation opens the sidebar instead
export default function MoreScreen() {
    return <View />;
}
